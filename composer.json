{
    "name": "telegram-bot/webhook",
    "description": "Telegram Bot Webhook",
    "type": "project",
    "require": {
        "php": ">=8.0"
    },
    "autoload": {
        "files": ["index.php"]
    },
    "scripts": {
        "post-install-cmd": [
            "chmod 777 users.json error.log"
        ]
    }
}